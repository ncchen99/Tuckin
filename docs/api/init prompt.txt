兩天 18 小時前可預約 早上 6 點

接著配對 早上 6 點 發出通知

有 11 小時可以確認 

距離開始兩天 7 小時前必須答覆 (下午五點前，最後一個確認必須定位)

接下來有 13 小時可以尋找空位

剩下 1 天又 18 小時十會確認


ex 預約週四晚餐

週二早上 6:00 截止

週二早上 6:20 公布名單

週二晚上 5 點前必須確認

如果有尚未滿足四人的桌子 

接著分兩輪尋找 17 ~ 06 、 06 ~ 13 。

一輪兩次通知

分組名單是由後端的運算產生

前端只能讀取分組名單

他也可以透過分組名單 確認這個小組內其他人確認了沒

完全匿名名單(userid? 每周都重新產生新的user ID代號)


預約截止的時間還有確認截止的時間都是記錄在資料庫的表格中由 edge function 去處理的


群組管理模組
餐廳管理模組
通知模組
用戶資料處理模組
通用工具














./api 我想請你在這個資料夾中幫我建立fast API 
並且幫我建立架構的檔案
我想要將API分成5個模組:

群組管理模組
餐廳管理模組
通知模組
用戶資料處理模組
通用工具


這是一個專門為學生打造的免費交友與聚餐平台

(已經進行一部份)我們使用 supabase 作為資料庫，並且使用 Firebase Cloud Messaging 與flutter應用 trigger 通知
我們的登入也是使用supabase 的 auth 串聯Google auth 登入 
這部分我們是完全使用 supabase 作為 baas 資料庫某一部分也是設定了RLS規則使得簡單的資料存取是直接在flutter應用上面進行的

(未來規劃)取得餐廳資訊會使用 Google的place API ，並且將需要用到的資料存到supabase資料庫，邊疆取得的餐廳縮圖存到 Cloudflare R2

現在我想要請你建立的API 用途是 for 一些需要處理敏感資料還有發送通知的程式碼 以及處理那一些使用者不能修改的表格


我會在圖片提供給你現有的資料庫table 
並且我想請你新增一些table:


1. users_profiles 的 view (給裝置端使用者存取使用的，使得使用者提供想查詢的user ID可以查詢，但無法直接查詢整個表)
   + user_id
   + nickname
   + personal_desc

2. uuid 與 groupid mapping 表，允許用戶使用 groupid 反查 group 中其他人的 uuid
2. **dining_events** - 聚餐事件 (日期、時間、狀態、用戶)
4. **restaurants** - 餐廳資訊 (類型、地址、營業時間、縮圖地址)
5. **restaurant_votes** - 群組餐廳投票記錄
6. **ratings** - 評分記錄 (包含不舒服評分)
8. **matching_scores** - 配對分數記錄 (儲存優先順序相關分數) {後期開發才會用到}

這些表格還沒有建立你可以幫我規劃欄位(使用最精簡的設計就好) 並且產生 sql 指令嗎 還有 python 的schemas

請你先幫我建立這一些規格就好先不要撰寫邏輯
 

 **餐廳選擇頁面** (restaurant_selection_page.dart)
   - 推薦餐廳卡，兩張卡片 (由 api 接收，請你先幫我用範例資料填充，upsplash)，上下排列，包含：餐廳縮圖、名稱、餐廳種類、地址(可點擊)，卡片點擊後可選擇出現橘色邊框
   - 以及下方第三張卡片，顯示"我想推薦餐廳"的可點擊卡片。(稍後完成推薦餐廳頁面)
   - 最下方，送出按鈕




**用戶資料與設定頁面** 
   - 更改基本資料
   - 更改飲食偏好
   - 重新進行個性測驗
   - 聚餐歷史記錄
   - 登出
   - 刪除帳號
   - 隱私與條款
   - 關於我們(連結：https://github.com/ncchen99/Tuckin)


